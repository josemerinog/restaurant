<%= form_for(@order) do  |f| %>
  <%= f.label :board %>
  <%= f.text_field :board %>
  <%= f.label :user_id %>
  <%= collection_select(:order, :user_id, User.all, :id, :email, prompt: true) %>

  <%= f.submit %>
<% end %>
